import { useUser, useSupabaseClient } from '@supabase/auth-helpers-react';
import React, {useState} from 'react';

export default function Home() {
/*   const user = useUser();
  const supabase = useSupabaseClient();
 */
  return (
    <div>
{/*       {user === null ? <div>Not signed in</div>
        : <div>Hello</div>} */}
    </div>
  )
}
